# launcher/
> L2 | 父级: ../CLAUDE.md

成员清单
ccv-launcher.ts: cc-viewer 启动器，负责 CLI 路径解析、启动参数映射、进程启动、URL 捕获、环境继承与退出监督
process-supervisor.ts: 进程监督工具，负责自身启动子进程的停止句柄、退出监听绑定与优雅停止后的强制终止收敛

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
